# My Expenses

A simple web app for tracking daily expenses.  
Built with HTML, CSS and JavaScript, and storing data using `localStorage`.

## Features
- Add expenses (place, amount, date)
  • If the date is left empty, the current date is used automatically
- Delete expenses
- Automatic saving in localStorage
- Total amount calculation
